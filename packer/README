On an EC2 instance with packer installed from packer.io execute the following command in this directory:

```
sudo packer build template.json
```

An AMI will be created. Create an instance from that AMI with the following:

```
aws ec2 run-instances --image-id <ami-id> --key-name philagov \
--subnet-id <vpc-subnet-id> --instance-type t2.micro --associate-public-ip-address
aws ec2 create-tags --resources <instance-id> --tags Key=Name,Value=phila.gov
```
